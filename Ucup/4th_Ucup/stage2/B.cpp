#include<bits/stdc++.h>

using ll = long long;
using namespace std;

constexpr int N = 1e5 + 10;

struct Point {
    ll x,y;
}

vector<Point> build(vector<Point> p){
    vector<point>stk;
    if(p.empty())return stk;
    sort(p.begin(),p.end());
    const auto check = [](const vector<Point> & stk,const Point & u){
        
    };
}

void solve()
{
    
}

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    int tt = 1;
    cin>>tt;
    while(tt--){
        solve();
    }
    return 0;
}