我要对拍，请帮我写一个数据生成器，要求：
1. 使用cpp
2. 不用命令行参数，在程序中显示写出各个参数的调节范围
3. 使用mt19937 g(chrono::steady_clock::now().time_since_epoch().count()); 等现代随机数生成器
4. 尽可能加入一些方便我对拍的调节参数(如生成字符串，可以用参数限制字符集的大小)
5. 输出到stdout
下面是题目：