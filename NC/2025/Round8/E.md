对于询问 $q = S$
根据min-max反演， 求
$$E(max\{S\}) = \sum_{T \subseteq S} E(min \{T\})$$
这里的$S,T$不是格子集，而是礼物集，不然集合太大了
要先预处理礼物集$min$期望，需要对应的格子集进行计算。
求解$E(min \{T\})$
假设 $T = \{g_1,g_2,...g_t\}$
最少一个被填满，再用一遍min-max反演？
这么做又回去了qwq



换一个反演方法？
设$F(S)$是最后一步刚好取满$S$的期望
$G(S)$是至少取完$S$且最后一步多了一个礼物的期望
有
$$G(S) = \sum_{T \subseteq S}F(T)$$
子集反演：
$$F(S) = \sum_{T \supseteq S} (-1)^{|T| - |S|}G(T)$$

$G(S)$或许是好做的。
但是好像并非好做。
相关问题：带权彩票收集问题。

去dp？dp表示抽到了这些
$dp[S] = \sum_{x \in S}dp[S - x] \times p(x)$

再换一个反演？
设$F(S)$是最后一步刚好取满$S$的期望
设$G(S)$是最后一步刚好取满$S$,且没有取其他线的期望
然后有$$F(S) = \sum_{T \supseteq S}G(T) ?$$



