
## prompt
我做出来了C题，但是有两个没完全明白的推理过程需要你教我一下：
1.  固定$b$，合法的$a$是数量是$\frac{(b - \gcd(P + 1, b))}{2}$,  这是我大力观察出来的,思考过程如下：
    1. 大部分符合0，0，1，1，2，2...这样的分布
    2. 观察到某些点为0 
    3. 发现这些点P mod b = -1 
    4. 寻求其他非零异常点的分析方法，尝试使用gcd来拟合(前面也推出来过gcd，可能和贝祖定理有关？但是后面发现推错了)
    5. 得到正确式子并成功验证
   
   怎么严格推理出来这个式子呢？
2. 最后需要求 
   $$\sum_{i = 1}^n \gcd(i, P + 1)$$
   我知道$f_N(x) = \gcd(x,N)$ 是一个积性函数，因此分解质因子后，枚举因子即可，(P + 1)因子数不会太多(估的，也不严格)
   然后怎么求解呢？利用恒等式$Id=\phi * I$, 也就是对于单点每一个因子处贡献$\phi(x)$即可，然后就可以枚举因子进行加速求解了。
   最后有一个推导结果：
   $$\sum_{i = 1}^n \gcd(i, P + 1) = \sum_{d | (P + 1)} \lfloor\frac{n}{d}\rfloor \phi(d)$$
   这个式子可以用其他方式推出来吗？
我还没有看题解，请解答一下我的问题。如果我们的解题思路不同，也请你对比一下。
我的打表探索和实现是D:\XCPC_Training\NC\2026\SoyCup3下的C.cpp和C2.cpp
