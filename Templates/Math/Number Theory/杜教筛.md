
能被杜教筛筛的函数可能和$\phi,\mu$相关，所以保留筛$\phi,\mu$
如$$f(x) = x^k\phi(x), g(x) = x ^ k \\ h(x) = \sum_{d|n}f(d)g(\frac{n}{d}) = n^k\sum_{d|n}\phi(d) = n^{k+1}$$
```c++
constexpr int N = 1664510 + 500;//(INT_MAX)^0.67 + 500;
vector<int>prime;
bitset<N + 1>isprime;
vector<int>mu;
vector<int>lpfp;
vector<int>phi;
vector<ll>f
unordered_map<ll, ll>pf;

void seive()
{
    isprime.set();
    mu.resize(N + 1);
    lpfp.resize(N + 1);
    phi.resize(N + 1);
    isprime[0] = isprime[1] = 0;
    phi[1] = lpfp[1] = mu[1] = 1;
    for (int i = 2; i <= N; i++) {
        if (isprime[i]) {
            prime.push_back(i);
            mu[i] = -1;
            lpfp[i] = i;
            phi[i] = i - 1;
        }
        for (auto& p : prime) {//p = lpf[i * p]
            if (p * i > N)
                break;
            isprime.flip(i * p);

            if (i % p == 0) {
                lpfp[i * p] = lpfp[i] * p;
                if (lpfp[i * p] == i * p) {//p ^ k
                    phi[i * p] = lpfp[i * p] - lpfp[i];//phi(p^k) = p^k - p^(k-1)
                }
                else {//p^k * ...
                    phi[i * p] = phi[lpfp[i * p]] * phi[(i * p) / lpfp[i * p]];
                }
                mu[i * p] = 0;
                break;
            }
            else {
                lpfp[i * p] = p;
                phi[i * p] = phi[i] * phi[p];
                mu[i * p] = mu[i] * (-1);
            }
        }
    }

    for (int i = 1; i <= N; i++) {
        f[i] = /**/
        pf[i] = pf[i-1] + f[i];
    }
}

// h = f * g
ll get_pf(ll n)
{
    if (n <= N) {
        return pf[n];
    }
    else if (pfmp.count(n)) {
        return pfmp[n];
    }
    else {
        ll res = /* ph(n) */;
        for (ll l = 2, r; l <= n; l = r + 1) {
            r = n / (n / l);
            res -= (/* pg(r) - pg(l - 1)*/) * get_pphi(n / l);
        }
        return pfmp[n] = res;
    }
}
```