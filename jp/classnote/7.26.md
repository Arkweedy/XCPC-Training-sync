1. set : 去重+排序特性 ： *打表好用* 
   eg:枚举三角形
2. queue用途 + 拓扑排序
3. pq与set的用法区别 ： 是否需要动态修改，pq可能要记录最新信息
4. set的其他注意点 ： 二分时，调用set::lower_bound ,而不是std::lower_bound
5. vector怎么更快？ 如果连续push_back，且知道push_back后的size，可以先reserve
6. vector resize,assign,reserve的区别
7. stack的使用：底层是deque，比vector慢，不如直接用vector
8. pq小根堆 ： priorty_queue<int,vector<int>,greater<int>>